class Solution {
    public String smallestSubsequence(String s) {

        Stack<Integer> st = new Stack<>();
        for(int i = 0; i<s.length(); i++){
            char ch = charAt(i);
            int n = (int)ch;
            if(n)
        } 
    }
}
